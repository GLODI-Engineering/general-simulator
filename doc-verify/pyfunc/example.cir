* PyFunction component reference -- the "## Example" netlist verbatim. Needs a --features
* python build of the CLI. No title line -- see doc-verify/pid/example.cir's own comment.
SRC kind=const value=3
* G1 calls add_one(SRC) every step (see add.py). Expect G1 = 4 for SRC = 3, every row.
G1 kind=pyfunc path=doc-verify/pyfunc/add.py function=add_one in=SRC
