[package]
name = "continuous-blocks"
version = "0.3.0"
description = "Continuous-time block-diagram elements (transfer function, state-space, PID, math ops) compiled into descriptor-DAE fragments of the same A*x + K*dx/dt = B*u shape general-mna already uses for circuits."
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/continuous-blocks"
keywords = ["control-systems", "state-space", "pid", "simulation"]
categories = ["science", "simulation"]

[dependencies]
