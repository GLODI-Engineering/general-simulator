[package]
name = "lcp-solver"
version = "0.3.1"
description = "Lemke's algorithm for the Linear Complementarity Problem (LCP), used by general-simulator to select which piecewise-linear device segment is active each timestep instead of Newton-Raphson + voltage limiting."
edition.workspace = true
license.workspace = true
readme = "README.md"
publish.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/lcp-solver"
keywords = ["lcp", "complementarity", "numerical"]
categories = ["algorithms", "science"]

[dependencies]
