[package]
name = "lcp-solver"
version = "0.1.0"
description = "Lemke's algorithm for the Linear Complementarity Problem (LCP), used by elspice-pwl to select which piecewise-linear device segment is active each timestep instead of Newton-Raphson + voltage limiting."
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
