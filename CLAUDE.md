# Claude Code project notes

Read [`AGENTS.md`](AGENTS.md) first. Project skills live under `.claude/skills/` once
populated. There is no pre-commit hook installation yet in this repo (unlike `general-mna`) —
run the gates in `AGENTS.md` manually until that's set up.
